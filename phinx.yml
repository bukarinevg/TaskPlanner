paths:
    migrations: db/migrations

environments:
    default_migration_table: phinxlog
    default_database: planner
    development:
        adapter: mysql
        host: planner-hostname
        name: planner
        user: user
        pass: user
        port: 3306
        charset: utf8